# 🚀 خطة التنفيذ الشاملة - Session الطويلة

## المرحلة 1: البنية التحتية ✅ (مكتملة)
- [x] API رفع الصور
- [x] تحديث Schema
- [x] إزالة التقييمات التجريبية
- [x] شريط الأقسام (9 + المزيد)

---

## المرحلة 2: الإعدادات والإدارة (P0)

### ملف: `/app/app/admin/settings/page.js`
**المشكلة**: لا يحفظ الإعدادات بشكل صحيح

**المطلوب**:
1. إصلاح حفظ روابط التواصل الاجتماعي
2. إصلاح رفع صور السلايدر (استخدام upload API)
3. إصلاح إضافة العملاء المميزين
4. إصلاح إضافة الأسئلة الشائعة

**التنفيذ**:
- تحديث نموذج الإعدادات
- دمج upload API
- إصلاح API endpoint للإعدادات

---

## المرحلة 3: إدارة المنتجات

### أ) تعديل المنتج
**ملف**: `/app/app/admin/products/page.js`

**المطلوب**:
1. زر "تعديل" يفتح نموذج التعديل
2. تحميل بيانات المنتج في النموذج
3. حفظ التعديلات

**التنفيذ**:
```javascript
- إضافة state للمنتج المحدد
- نموذج يمكن إعادة استخدامه (إضافة/تعديل)
- API PUT للتحديث
```

### ب) دمج رفع الصور
**المطلوب**: استخدام upload API في نموذج المنتج

---

## المرحلة 4: إدارة الأقسام

### ملف: `/app/app/admin/categories/page.js`

**المطلوب**:
1. Toggle switch لكل قسم (active/inactive)
2. تحديث API للأقسام
3. فلترة الأقسام النشطة في الواجهة

**التنفيذ**:
```javascript
// API Update
PATCH /api/categories/:id
{ status: 'active' | 'inactive' }

// Frontend
<Toggle checked={cat.status === 'active'} onChange={...} />
```

---

## المرحلة 5: أكواد الخصم

### ملف: `/app/app/admin/discounts/page.js`

**المطلوب**:
1. زر "تعديل" لكل كود
2. إظهار عدد مرات الاستخدام
3. نموذج تعديل الكود

**التنفيذ**:
- إضافة عمود "الاستخدامات"
- حساب عدد الطلبات التي استخدمت الكود
- نموذج تعديل مع تحميل البيانات

---

## المرحلة 6: صفحة المنتج (Frontend)

### ملف: `/app/app/product/[slug]/page.js`

**المطلوب**:
1. عرض خيارات التخصيص:
   - اختيار اللون
   - إدخال نص
   - رفع شعار أو إدخال اسم
2. حفظ التخصيصات مع الطلب

**التنفيذ**:
```javascript
// عرض التخصيصات
{product.customizations?.map(custom => {
  if (custom.type === 'logo') {
    return <LogoUploadOrText />
  }
  if (custom.type === 'color') {
    return <ColorPicker />
  }
  if (custom.type === 'text') {
    return <TextInput />
  }
})}

// حفظ مع الطلب
localStorage.setItem('cart', JSON.stringify([{
  ...item,
  customizations: selectedCustomizations
}]))
```

---

## المرحلة 7: تفاصيل الطلب

### أ) صفحة الطلب (User)
**ملف**: `/app/app/orders/[id]/page.js`

**المطلوب**:
1. إظهار كود الخصم المستخدم
2. إظهار عدد النقاط المستخدمة
3. إظهار التخصيصات (لون، نص، شعار)
4. زر "رجوع للرئيسية"
5. زر "تقييم المنتج" (إذا delivered)

### ب) صفحة الطلب (Admin)
**ملف**: `/app/app/admin/orders/[id]/page.js`

**المطلوب**:
نفس المعلومات + إمكانية تحميل الشعار المرفوع

---

## المرحلة 8: نظام التقييمات

### ملف: `/app/app/orders/[id]/page.js`

**المطلوب**:
```javascript
{order.deliveryStatus === 'delivered' && (
  <button onClick={openReviewModal}>
    ⭐ تقييم المنتج
  </button>
)}

// Modal
<ReviewModal 
  orderId={order.id}
  productId={product.id}
  onSubmit={submitReview}
/>
```

---

## المرحلة 9: التحسينات النهائية

### أ) أزرار الرجوع
- صفحة الطلبات: زر "رجوع للرئيسية"
- جميع الصفحات الداخلية: زر "رجوع"

### ب) فلترة الأقسام
- تطبيق فلتر `status: 'active'` في جميع الصفحات
- شريط الأقسام
- صفحة القسم
- صفحة المنتج

---

## 📊 جدول التنفيذ

| المرحلة | المهام | الوقت المقدر | الحالة |
|---------|--------|--------------|--------|
| 1 | البنية التحتية | 30 دقيقة | ✅ مكتمل |
| 2 | الإعدادات | 60 دقيقة | 🔄 جاري |
| 3 | إدارة المنتجات | 45 دقيقة | ⏳ |
| 4 | إدارة الأقسام | 30 دقيقة | ⏳ |
| 5 | أكواد الخصم | 30 دقيقة | ⏳ |
| 6 | صفحة المنتج | 60 دقيقة | ⏳ |
| 7 | تفاصيل الطلب | 45 دقيقة | ⏳ |
| 8 | التقييمات | 30 دقيقة | ⏳ |
| 9 | التحسينات | 30 دقيقة | ⏳ |

**الإجمالي**: ~6 ساعات

---

## 🎯 الأولوية الآن

سأبدأ بـ:
1. ✅ إصلاح الإعدادات (روابط، عملاء، FAQs)
2. ✅ إصلاح تعديل المنتج
3. ✅ Toggle الأقسام
4. ✅ التخصيصات في المنتج
5. ✅ تفاصيل الطلب
6. ✅ زر التقييم
7. ✅ أزرار الرجوع
8. ✅ دمج upload API
9. ✅ اختبار شامل
